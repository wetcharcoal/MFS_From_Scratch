# Copy to .env.ic.local (gitignored) to set mainnet canister IDs without editing committed .env.ic.
# Vite applies .env.ic.local after .env.ic — highest precedence for mode `ic`.

VITE_CANISTER_ID_ASEED_BACKEND=your-backend-canister-id
VITE_CANISTER_ID_ASEED_FRONTEND=your-frontend-canister-id
