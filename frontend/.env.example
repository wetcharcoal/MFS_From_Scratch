# Local dev: copy to .env.local (see .env.local.example for dev_key lane).
# Mainnet bundle: use `npm run build:ic` — reads `.env.ic` and optional `.env.ic.local` (overrides .env.local).

VITE_CANISTER_ID_ASEED_BACKEND=rrkah-fqaaa-aaaaa-aaaaq-cai
VITE_CANISTER_ID_ASEED_FRONTEND=YOUR_ASEED_FRONTEND_CANISTER_ID
VITE_AUTH_MODE=ii
VITE_II_PROVIDER=https://id.ai
VITE_DFX_NETWORK=local
VITE_DFX_HOST=http://127.0.0.1:4943
DFX_HOST=http://127.0.0.1:4943
